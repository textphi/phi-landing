# Rho Landing — Agent Context

This repo (`rho-landing`) is the **marketing landing page** for Rho — a Next.js site deployed on Vercel.

The **actual product** (backend, agent, infra) lives in a separate repo: **`rho-agent`**. For product details, features, and the full stack, look there.

## Docs

- `product.md` — what Rho is and who it's for.
- `tech_stack.md` — this site's stack (Next.js + Vercel).
