# Phi Landing — Agent Context

This repo (`phi-landing`) is the **marketing landing page** for Phi — a Next.js site deployed on Vercel.

The **actual product** (backend, agent, infra) lives in a separate repo: **`phi-agent`**. For product details, features, and the full stack, look there.

## Docs

- `product.md` — what Phi is and who it's for.
- `tech_stack.md` — this site's stack (Next.js + Vercel).
