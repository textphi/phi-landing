# Tech Stack — phi-landing

- **Next.js** — the landing page framework (React).
- **Vercel** — hosting and deployment.

That's it. This repo is just the marketing site; the product stack lives in `phi-agent`.
